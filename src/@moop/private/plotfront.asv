function plotfronts(fits front, notfront, dims)
% visualize sorted non-dominated fronts for NSGA-II

PAUSE_SECONDS = 1.0;
FONTSIZE = 12;
COLOR_FRONT = 'r';
COLOR_NOTFRONT = 'b';
SYMBOL_FRONT = '=

clf
hold on
axis equal
axis([0 1 0 1])

plotfits(fits, front, COLOR_FRONT)
plotfits(fits, notfront, COLOR_NOTFRONT)

pause(PAUSE_SECONDS)

function plotfits(fits, indices, color)

for i = 1:length(indices)
    fit = fits{indices(i)};
    x = fit(dims(1));
    y = fit(dims(2));
    plot(
end

